import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.pokedex.mobile',
  appName: 'pokedex-mobile',
  webDir: 'dist/pokedex-mobile/browser'
};

export default config;
